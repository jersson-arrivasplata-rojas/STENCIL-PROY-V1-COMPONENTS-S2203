export default async () => {
  /**
   * The code to be executed should be placed within a default function that is
   * exported by the global script. Ensure all of the code in the global script
   * is wrapped in the function() that is exported.
   */
};
/**


  function socialMediaLinkedin(url) {
    window.open(`https://www.linkedin.com/sharing/share-offsite/?url=${url}`, 'sharer', 'toolbar=0,status=0,width=626,height=436');
    return false;
  }

  function socialMediaFacebook(url, description) {
    var pageTitle = String(description).replace(/\&/g, '%26');
    window.open(`http://www.facebook.com/sharer.php?u=${url}&quote=${pageTitle}`, 'sharer', 'toolbar=0,status=0,width=626,height=436');
    return false;
  }

  function socialMediaWhatsapp(url) {
    window.open(`https://api.whatsapp.com/send?phone=&text=${url}`);
    return false;
  }




 */